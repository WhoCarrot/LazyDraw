const canvasForeground = require('./foreground');
const canvasBackground = require('./background');
const canvasHover = require('./hover');

module.exports = {
    canvasForeground,
    canvasBackground,
    canvasHover
};
