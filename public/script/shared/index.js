const { lazyBrush, opponentBrush } = require('./brush');
const strokes = require('./strokes');
const IoClient = require('./socket');

module.exports = {
    lazyBrush,
    opponentBrush,
    strokes,
    IoClient
};
