const LazyBrush = require('./lazybrush');
const LazyPoint = require('./lazypoint');
const Stroke = require('./stroke');
const Point = require('./point');

module.exports = {
    LazyBrush,
    LazyPoint,
    Stroke,
    Point
};
